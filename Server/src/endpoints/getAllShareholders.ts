// import { shareHolders } from '../types/types';
// import { Request, Response } from "express";
// import connection from '../connection';





// export default async function getAllShareHolders(
// req:Request,
// res:Response
// ):Promise <void>{
    
    
//     try {
//         const name = req.query
//         // const result:  shareHolders[] = await connection.raw("Select * FROM SHAREHOLDER")
//         // res.send(result[0])

//         const shareHolder: shareHolders[] = await connection("SHAREHOLDERS")

//         res.send(shareHolder)

//     } catch (error) {
//         res.status(500).send("Unexpected server error")
        
//     }
       
// }